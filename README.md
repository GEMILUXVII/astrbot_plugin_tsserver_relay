# AstrBot TeamSpeak 3 服务器监控插件

实时监控 TeamSpeak 3 服务器，推送用户进出通知和服务器状态到群消息。

## 功能特性

- 🔔 **实时通知**: 用户加入/离开服务器时自动推送通知
- 📊 **定时状态**: 每小时自动推送服务器状态（可配置）
- 🖥️ **多服务器**: 支持监控多个 TS3 服务器
- 👥 **订阅管理**: 群组独立订阅，灵活控制通知

## 命令列表

### 管理员命令

| 命令 | 说明 |
|------|------|
| `/ts add <别名> <主机> <用户名> <密码> [端口] [虚拟服务器ID]` | 添加 TS3 服务器 |
| `/ts del <别名>` | 删除服务器 |
| `/ts restart [别名]` | 重启监控 |
| `/ts interval <别名> <分钟>` | 设置状态推送间隔 |
| `/ts join <别名> [on/off]` | 切换加入通知 |
| `/ts leave <别名> [on/off]` | 切换离开通知 |

### 普通命令

| 命令 | 说明 |
|------|------|
| `/ts ls` | 查看监控列表 |
| `/ts status [别名]` | 查看服务器状态 |
| `/ts sub <别名>` | 订阅通知 |
| `/ts unsub <别名>` | 取消订阅 |
| `/ts mysub` | 查看我的订阅 |

## 配置要求

### TeamSpeak 3 服务器端

1. 启用 ServerQuery（默认端口 10011）
2. 创建 ServerQuery 账户
3. 确保账户具有以下权限：
   - `b_serverquery_login`
   - `b_virtualserver_client_list`
   - `b_virtualserver_notify_register`

## 安装

1. 将本插件放入 AstrBot 的 `data/plugins/` 目录
2. 安装依赖：`pip install ts3`
3. 重启 AstrBot
4. 使用 `/ts add` 命令添加服务器

## 使用示例

```
# 添加服务器
/ts add myserver ts3.example.com serveradmin password123

# 订阅通知
/ts sub myserver

# 查看状态
/ts status myserver
```

## 许可证

MIT License
